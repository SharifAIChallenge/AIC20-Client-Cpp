#ifndef AIC20_CLIENT_CPP_ENUMS_H
#define AIC20_CLIENT_CPP_ENUMS_H

enum UnitTarget {
        AIR,
        GROUND,
        BOTH
};

enum SpellTarget {
    SELF,
    ALLIED,
    ENEMY
};

enum SpellType {
    HP,
    TELE,
    DUPLICATE,
    HASTE
};


#endif //AIC20_CLIENT_CPP_ENUMS_H
